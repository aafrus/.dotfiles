#!/bin/bash
case "$1" in
    up)   brightnessctl set 5%+ ;;
    down) brightnessctl set 5%- ;;
esac

LEVEL=$(brightnessctl get)
MAX=$(brightnessctl max)
PCT=$(( LEVEL * 100 / MAX ))

notify-send -t 1500 -r 8882 "Ljusstyrka" "${PCT}%"
