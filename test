/* See LICENSE file for copyright and license details. */

/* Appearance */
static const unsigned int borderpx  = 0;        /* Ingen pixel border */
static const unsigned int snap      = 32;       /* Snap pixel */
static const int showbar            = 0;        /* Ingen statusbar */
static const int topbar             = 1;        /* Bar längst upp (om den används) */
static const char *fonts[]          = { "monospace:size=10" };
static const char col_gray1[]       = "#222222"; /* Bakgrundsfärg */
static const char col_gray2[]       = "#444444";
static const char col_gray3[]       = "#bbbbbb";
static const char col_gray4[]       = "#eeeeee";
static const char col_cyan[]        = "#005577"; /* Accentfärg */

/* Terminal */
static const char *termcmd[] = { "st", "-c", "st-256color", "-e", "sh", NULL }; /* Svart terminal */

/* Keybindings */
static const char *dmenucmd[] = { "dmenu_run", NULL }; /* dmenu */
static const char *firefoxcmd[] = { "firefox", NULL }; /* Firefox */

static Key keys[] = {
    /* Modifier    Key          Function        Argument */
    { MODKEY,                       XK_Return, spawn,          {.v = termcmd } }, /* Alt+Enter: Terminal */
    { MODKEY,                       XK_s,      togglefloating, {0} },             /* Alt+s: Floating mode */
    { MODKEY,                       XK_d,      spawn,          {.v = dmenucmd } }, /* Alt+d: dmenu */
    { MODKEY,                       XK_w,      spawn,          {.v = firefoxcmd } }, /* Alt+w: Firefox */
    { MODKEY,                       XK_h,      focusstack,     {.i = +1 } },      /* Alt+h: Fokusera vänster */
    { MODKEY,                       XK_l,      focusstack,     {.i = -1 } },      /* Alt+l: Fokusera höger */
    { MODKEY|ShiftMask,             XK_h,      movestack,      {.i = +1 } },      /* Alt+Shift+h: Flytta fönster vänster */
    { MODKEY|ShiftMask,             XK_l,      movestack,      {.i = -1 } },      /* Alt+Shift+l: Flytta fönster höger */
    { MODKEY|ShiftMask,             XK_j,      movestack,      {.i = +2 } },      /* Alt+Shift+j: Flytta fönster ner */
    { MODKEY|ShiftMask,             XK_k,      movestack,      {.i = -2 } },      /* Alt+Shift+k: Flytta fönster upp */
    { MODKEY|ControlMask,           XK_h,      setmfact,       {.f = -0.05} },    /* Alt+Ctrl+h: Minska bredd */
    { MODKEY|ControlMask,           XK_l,      setmfact,       {.f = +0.05} },    /* Alt+Ctrl+l: Öka bredd */
    { MODKEY|ControlMask,           XK_j,      setcfact,       {.f = -0.05} },    /* Alt+Ctrl+j: Minska höjd */
    { MODKEY|ControlMask,           XK_k,      setcfact,       {.f = +0.05} },    /* Alt+Ctrl+k: Öka höjd */
    { MODKEY,                       XK_grave,  zoom,           {0} },             /* Alt+`: Zoom in */
    { MODKEY,                       XK_plus,   zoom,           {0} },             /* Alt++: Zoom out */
};

/* Layouts */
static const Layout layouts[] = {
    { "[]=",      tile },    /* Master stack layout */
};

/* Tags */
static const char *tags[] = { "1", "2", "3", "4", "5", "6", "7", "8", "9" };

/* Rules */
static const Rule rules[] = {
    /* xprop(1):
     * WM_CLASS(STRING) = instance, class
     * WM_NAME(STRING) = title
     */
    { "st-256color", NULL, NULL, 0, 1, -1 }, /* Alla terminaler i master */
};

/* Monitor setup */
static const MonitorRule monrules[] = {
    { "VGA-1",  { 0, 0, 1920, 1080 } }, /* Exempel på monitorinställningar */
};

/* Other settings */
static const int resizehints = 0; /* 1 means respect size hints in tiled resizals */
static const int lockfullscreen = 1; /* 1 will force focus on the fullscreen window */
