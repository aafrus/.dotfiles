# Define the master-and-stack layout
master_and_stack="(split vertical:0.5:0 (clients max:0) (split horizontal:0.5:0 (clients max:0) (clients max:0)))"

# Apply this layout to all tags
tag_names=( {0..4} )
for i in "${!tag_names[@]}"; do
    hc load "${i}" "${master_and_stack}"
done
