#!/usr/bin/env zsh

git_current_branch() {
  git branch --show-current 2>/dev/null || echo "detached"
}

git_main_branch() {
  git config --get gitflow.branch.main 2>/dev/null || 
  { git branch -l | grep -E '^\*? *(main|master)' | sed 's/^* *//'; }
}
